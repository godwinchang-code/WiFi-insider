[package]
name = "wifi-insider"
version = "0.1.0"
edition = "2021"
authors = ["WiFi-insider Team"]
description = "Wi-Fi 7 packet analyzer for OpenWrt - detects PHY/MAC/EHT issues from pcap files"
license = "MIT"

[dependencies]
# PCAP parsing
pcap = "1.3"
pcap-file = "2.0"

# Packet parsing
pnet = "0.34"
pnet_macros_support = "0.34"

# Serialization and data structures
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"

# CLI and logging
clap = { version = "4.5", features = ["derive"] }
env_logger = "0.11"
log = "0.4"

# Statistics and analysis
ordered-float = "4.2"

# Error handling
anyhow = "1.0"
thiserror = "1.0"

[dev-dependencies]
# Testing
tempfile = "3.8"
criterion = "0.5"
mockall = "0.12"
pretty_assertions = "1.4"
hex = "0.4"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true

[profile.dev]
opt-level = 0

[lib]
name = "wifi_insider"
path = "src/lib.rs"

[[bin]]
name = "wifi-insider"
path = "src/main.rs"

[[bench]]
name = "packet_processing"
harness = false
